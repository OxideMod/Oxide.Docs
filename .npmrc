# Refuse to install package versions published less than 7 days ago, reducing
# exposure to freshly-published compromised releases. Requires npm >= 11.10
# (pinned via "packageManager"/"volta" in package.json).
min-release-age=7
